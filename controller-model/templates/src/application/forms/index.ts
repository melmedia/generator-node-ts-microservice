import { Create<%= entityName %> } from './Create<%= entityName %>';
import { Update<%= entityName %> } from './Update<%= entityName %>';

export {
  Create<%= entityName %>,
  Update<%= entityName %>,
};
